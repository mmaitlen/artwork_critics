import 'package:flutter/material.dart';

class CritiquePage extends StatelessWidget {
  const CritiquePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Art Critique — coming soon'),
      ),
    );
  }
}
